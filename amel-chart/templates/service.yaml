apiVersion: v1
kind: Service
metadata:
  name: {{ include "amel-chart.fullname" . }}
  labels:
    {{- include "amel-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "amel-chart.selectorLabels" . | nindent 4 }}
